spring:
  application:
    name: onebuy-ms-user
  datasource:
    url: jdbc:mariadb://localhost:3306/mservices
    username: root
    password: zhanghj811
    driver-class-name: com.mysql.jdbc.Driver

server:
  port: 8100

eureka:
  client:
    service-url:
      defaultZone: http://localhost:18080/eureka/

logging:
  file: logs/onebuy-ms-user.log